const projects = () => {
    return(
        <main>
            <div>
                <h1 className="text-4xl flex flex-col">Recycle bin shortcut</h1>
            </div>
        </main>
    )
}

export default projects;